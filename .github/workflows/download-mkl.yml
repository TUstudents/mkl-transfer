name: Legacy MKL transfer

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Legacy workflow retained for reference."
          echo "Use publish-runtime.yml for durable release assets."
          echo "Use rehydrate-runtime.yml to create a connector-friendly artifact."
